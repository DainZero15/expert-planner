import Link from'next/link';import{redirect}from'next/navigation';import{createClient}from'@/lib/supabase/server';import{add}from'./actions';import Form from'./form';export default async function Page(){const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const{data:experts}=await s.from('experts').select('*').order('name');return <main className="shell"><header className="topbar"><Link className="brand" href="/dashboard">Expert <span>Planner</span></Link></header><Form action={add}/><section className="card" style={{marginTop:20}}><h1>Experts</h1>{experts?.map(e=><p key={e.id}><strong>{e.name}</strong> · {e.start_time.slice(0,5)}–{e.end_time.slice(0,5)} · {e.default_visit_minutes} min bezoek</p>)||<p>Geen experts.</p>}</section></main>}
