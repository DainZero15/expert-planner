import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export default async function Dashboard() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); return <main className="shell"><header className="topbar"><div className="brand">Expert <span>Planner</span></div><span className="muted">{user.email}</span></header><section className="card"><div className="eyebrow">Expert Planner</div><h1>Uw werkdag plannen.</h1><p className="muted">Begin met klanten, experts en daarna de routeplanning.</p><div className="grid"><a className="card" href="/import"><h2>Importeren</h2><p className="muted">Vendit-bestand controleren en importeren.</p></a><a className="card" href="/customers"><h2>Klanten</h2><p className="muted">Klanten bekijken en beheren.</p></a><a className="card" href="/experts"><h2>Experts</h2><p className="muted">Werktijden, adressen en werkgebied instellen.</p></a></div></section></main>; }
